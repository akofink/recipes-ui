import React, { FC } from 'react';
import {
  Container,
  Row,
  Col,
} from 'react-bootstrap';

export const Recipes: FC = () => {
  return (
    <Container>
      <Row>
        <Col>Foo</Col>
        <Col>Foo</Col>
      </Row>
    </Container>
  );
};
